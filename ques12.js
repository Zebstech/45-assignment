let members = ['zubair', 'umair', 'aqib', 'amir', 'zahid',];
let message = ': kal birthday hai kis ki:';
for (let i = 0; i < members.length; i++) {
    console.log(message + members[i]);
}
export {};
